sns-sqs:----
access policy in sqs:

{
  "Version": "2012-10-17",
  "Id": "AllowSNSToSendMessage",
  "Statement": [
    {
      "Sid": "Allow-SNS-SendMessage",
      "Effect": "Allow",
      "Principal": {
        "Service": "sns.amazonaws.com"
      },
      "Action": "sqs:SendMessage",
      "Resource": "arn:aws:sqs:us-east-1:640270804884:sqs-internal",
      "Condition": {
        "ArnEquals": {
          "aws:SourceArn": "arn:aws:sns:us-east-1:640270804884:internal-2"
        }
      }
    }
  ]
}

lambda code:
import json

def lambda_handler(event, context):
    for record in event['Records']:
        body = json.loads(record['body'])
        print("SNS Message:")
        print(body['Message'])

